@model ProductDetailsModel
@if (Model.HasSampleDownload)
{
    <div class="download-sample">
        <a class="btn waves-effect waves-light" href="@Url.RouteUrl("GetSampleDownload", new { productid = Model.Id })">@T("Products.DownloadSample")</a>
    </div>
}